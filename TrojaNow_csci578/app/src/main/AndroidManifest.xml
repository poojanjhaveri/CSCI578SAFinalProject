<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.usc.trojanow.activities" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Accessing camera hardware -->
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >
        <activity
            android:name="edu.usc.trojanow.activities.LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.ActionBarTabActivity"
            android:label="@string/title_activity_feed" >
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.RegisterActivity"
            android:label="@string/title_activity_register" >
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.PostThoughtActivity"
            android:label="@string/title_activity_post_thought" >
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.PhotoActivity"
            android:label="@string/title_activity_photo" >
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.SensorActivity"
            android:label="Sensor" >
        </activity>
        <activity
            android:name="edu.usc.trojanow.activities.LocationActivity"
            android:label="@string/title_activity_location" >
        </activity>
    </application>

</manifest>
